Keeps public/ present in a fresh clone. The Dockerfile's runner stage does
COPY --from=builder /app/public ./public, which fails outright if the
directory does not exist -- and git does not track empty directories.
Phase 0's prep-images.ts will fill this with the optimised image variants.
